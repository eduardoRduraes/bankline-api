cors:
  originPatterns: "https://bankline-api-production-3838.up.railway.app, http://localhost:8080"
spring:
  application:
    name: bankline-api

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://roundhouse.proxy.rlwy.net:50724/railway
    username: postgres
    password: DkjXpnYivCUqTosddtXGGOFKEiidZrMR
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          lob:
            non-contextual-creation: true
        show_sql: true
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    use-root-path: true
